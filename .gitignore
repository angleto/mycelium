# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.eggs/
build/
dist/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# Env / secrets
.env
*.local

# Node / web
node_modules/
web/dist/
web/.vite/

# OS / editor
.DS_Store
*.swp

# Production deploy is an independent git repository (own history,
# its own remote). Flow tracks only deploy/local/ (dev infra, used by CI).
/deploy/prod/

# Local working files dropped in the repo root: PDFs we read for spec
# or invoice samples, and Claude Code's per-session metadata. Never
# part of the project.
*.pdf
.claude/
