# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.eggs/
build/
dist/

# Tooling caches
.ruff_cache/
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/

# Env / secrets
.env
*.local

# Node / web
node_modules/
web/dist/
web/.vite/

# OS / editor
.DS_Store
*.swp

# Reference locale, non versionato (richiesta utente)
docs/rag-architectures.txt

# Production deploy is an independent git repository (own history,
# its own remote). Flow tracks only deploy/local/ (dev infra, used by CI).
/deploy/prod/
