# Variabili per docker-compose (deploy/). Copia in deploy/.env.
POSTGRES_USER=flow
POSTGRES_PASSWORD=flow
POSTGRES_DB=flow
FLOW_DB_PORT=5432
FLOW_REDIS_PORT=6379
# Password del ruolo runtime flow_app (usata da `make db-bootstrap`).
FLOW_DB_APP_PASSWORD=flow_app
