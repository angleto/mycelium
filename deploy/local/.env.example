# Variables for docker-compose (deploy/). Copy to deploy/.env.
POSTGRES_USER=flow
POSTGRES_PASSWORD=flow
POSTGRES_DB=flow
MYCELIUM_DB_PORT=5432
MYCELIUM_REDIS_PORT=6379
# Runtime role flow_app password (used by `make db-bootstrap`).
MYCELIUM_DB_APP_PASSWORD=flow_app
