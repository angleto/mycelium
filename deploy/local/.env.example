# Variables for docker-compose (deploy/). Copy to deploy/.env.
POSTGRES_USER=mycelium
POSTGRES_PASSWORD=mycelium
POSTGRES_DB=mycelium
MYCELIUM_DB_PORT=5432
MYCELIUM_REDIS_PORT=6379
# Runtime role mycelium_app password (used by `make db-bootstrap`).
MYCELIUM_DB_APP_PASSWORD=mycelium_app
